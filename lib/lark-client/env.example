# ==========================================
# Lark Client Integration - Environment Setup
# ==========================================
#
# This module allows your client-side application to send messages to a Lark webhook.
# To protect your webhook URL and message prefixes from simple scrapers, we use
# basic obfuscation (Base64 for URLs, XOR for prefixes).
#
# Follow the instructions below to generate your secrets.

# ------------------------------------------
# 1. LARK WEBHOOK URL (Base64 Encoded)
# ------------------------------------------
# The full webhook URL provided by Lark/Feishu.
#
# GENERATION:
# Run this command in your terminal:
# echo -n "YOUR_ACTUAL_WEBHOOK_URL" | base64
#
# DECODING (For verification):
# echo "YOUR_BASE64_STRING" | base64 --decode
#
NEXT_PUBLIC_LARK_WEBHOOK_URL=

# ------------------------------------------
# 2. MESSAGE PREFIX (XOR Encoded)
# ------------------------------------------
# A custom tag added to the start of every message (e.g., specific user mentions).
# This is encrypted using a simple XOR cipher to be slightly harder to read than Base64.
#
# KEY: "aacexports" (Default key used in the code. Change in lib/lark-client/core.ts if needed)
#
# GENERATION:
# Run this Node.js one-liner:
# node -e 'const key="aacexports"; const secret="YOUR_PREFIX_TEXT"; let out=""; for(let i=0;i<secret.length;i++) out+=(secret.charCodeAt(i)^key.charCodeAt(i%key.length)).toString(16).padStart(2,"0"); console.log(out);'
#
# DECODING:
# You can use the `decryptXor` function in `lib/lark-client/core.ts` or reverse the script above.
#
NEXT_PUBLIC_LARK_MESSAGE_PREFIX=
